import 'package:flutter/material.dart';

class AppConstants {
  AppConstants._();

  static const Color initialBackgroundColor = Colors.white;
  static const Color lightTextColor = Colors.white;
  static const Color darkTextColor = Colors.black;

  static const Duration animationDuration = Duration(milliseconds: 500);

  static const double textFontSize = 32.0;

  static const double backgroundColorLuminance = 0.5;

  static const int maxColorValue = 255;
}
